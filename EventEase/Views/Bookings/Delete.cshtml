@model EventEase.Models.Booking

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Delete Booking";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Booking</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.EventId)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.EventId)</dd>

        <dt class="col-sm-2">@Html.DisplayNameFor(m => m.VenueId)</dt>
        <dd class="col-sm-10">@Html.DisplayFor(m => m.VenueId)</dd>

        
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="BookingId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
